{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid == $uid || (auth.token.email_verified == true && auth.token.admin == true))",
        ".write": "auth != null && auth.uid == $uid && (auth.token.email_verified == true || !data.exists())",
        "fcmTokens": {
          "$tokenId": {
            ".read": "auth != null && auth.uid == $uid",
            ".write": "auth != null && auth.uid == $uid && auth.token.email_verified == true",
            ".validate": "newData.hasChildren(['token', 'updatedAt']) && newData.child('token').isString() && newData.child('token').val().length > 0 && newData.child('updatedAt').isNumber()"
          }
        }
      }
    },
    "supportAdmins": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid"
      },
      ".write": false
    },
    "supportInbox": {
      ".read": "auth != null && auth.token.email_verified == true && auth.token.admin == true",
      ".write": false,
      "$userId": {
        ".read": "auth != null && auth.token.email_verified == true && auth.token.admin == true"
      }
    },
    "chats": {
      "$uid": {
        ".read": "auth != null && auth.token.email_verified == true && (auth.uid == $uid || auth.token.admin == true)",
        "conversations": {
          "$conversationId": {
            ".write": "auth != null && auth.token.email_verified == true && auth.uid == $uid && $conversationId == 'support' && newData.exists() && (!data.exists() || (newData.child('name').val() == data.child('name').val() && newData.child('role').val() == data.child('role').val() && newData.child('avatar').val() == data.child('avatar').val() && newData.child('online').val() == data.child('online').val() && (newData.child('unread').val() == data.child('unread').val() || newData.child('unread').val() == 0)))",
            ".validate": "newData.hasChildren(['name', 'role', 'avatar', 'lastMessage', 'lastMessageAt', 'unread', 'online']) && (data.exists() || (newData.child('name').val() == 'TaxPhil Support' && newData.child('role').val() == 'Customer Support Team' && newData.child('avatar').val() == 'TP' && newData.child('lastMessage').val() == '' && newData.child('unread').val() == 0 && newData.child('online').val() == false))",
            "name": {
              ".validate": "newData.isString() && newData.val().length <= 120"
            },
            "role": {
              ".validate": "newData.isString() && newData.val().length <= 120"
            },
            "avatar": {
              ".validate": "newData.isString() && newData.val().length <= 10"
            },
            "lastMessage": {
              ".validate": "newData.isString() && newData.val().length <= 4000"
            },
            "lastMessageAt": {
              ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= now + 60000"
            },
            "unread": {
              ".validate": "newData.isNumber() && newData.val() >= 0"
            },
            "online": {
              ".validate": "newData.isBoolean()"
            },
            "$other": {
              ".validate": false
            }
          }
        },
        "messages": {
          "$conversationId": {
            "$messageId": {
              ".write": "auth != null && auth.token.email_verified == true && auth.uid == $uid && $conversationId == 'support' && !data.exists() && newData.exists()",
              ".validate": "newData.hasChildren(['senderId', 'senderName', 'content', 'createdAt'])",
              "senderId": {
                ".validate": "newData.isString() && newData.val() == auth.uid"
              },
              "senderName": {
                ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 120"
              },
              "content": {
                ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 4000"
              },
              "createdAt": {
                ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= now + 60000"
              },
              "$other": {
                ".validate": false
              }
            }
          }
        }
      }
    }
  }
}
