{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid && auth.token.email_verified == true",
        ".write": "auth != null && auth.uid == $uid && (!data.exists() || auth.token.email_verified == true)"
      }
    }
  }
}
